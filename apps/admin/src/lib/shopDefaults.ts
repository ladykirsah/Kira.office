/**
 * Default Thai bill strings used when the shop hasn't set a custom value in Shop info.
 * Shared so the Shop-info form (as placeholders) and the bill (as fallbacks) stay in sync.
 */
export const SHOP_DEFAULTS = {
  quoteNote: "* ราคาประเมิน อาจเปลี่ยนแปลงตามหน้างาน",
  qrHeadline: "สนใจติดต่อร้าน",
  qrSubtitle: "สแกน QR เพื่อแชท / จองคิว ได้ทันที",
} as const;
